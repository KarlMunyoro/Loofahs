import React from "react";

function NavBar(){

    return(
        <nav id="NavBar">
        <a href="#home">Home</a>
        <a href="#about">About</a>
        <a href="#WhatsAppForm">Contacts</a>
        </nav>
    )
}

export default NavBar;